var LogInUser = (name) => {
    return `${name} has been logged in successfully!`
}

var SignUpUser =  (name) => {
    return `${name} has been signed up successfully!`
}

module.exports = { LogInUser, SignUpUser }